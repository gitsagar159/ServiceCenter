@model ServiceCenter.Models.AreaMaster
@{
    ViewBag.Title = "Area Form";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="layout-content">

    <!-- Content -->
    <div class="container-fluid flex-grow-1 container-p-y">

        <div class="card mb-4">
            <h6 class="card-header">
                Area Form
            </h6>
            <div class="card-body">
                @*<form id="frmArea">*@
                    @Html.HiddenFor(m => m.AreaId)
                    <div class="form-group">
                        <label class="form-label">Area Name</label>
                        @Html.TextBoxFor(m => m.AreaName, new { @class = "form-control", @placeholder = "Area Name" })
                        <small id="AreaNameToolTip" class=""></small>
                    </div>
                    <button id="btnsubmit"  class="btn btn-default"> Submit </button>
                @*</form>*@
                </div>
        </div>

    </div>
    <!-- / Content -->
    <!-- Layout footer -->
    @Html.Partial("FooterBar")
    <!-- / Layout footer -->

    @section scripts {

        <script src="~/Scripts/ProjectJs/MasterForms.js"></script>
        <script>

            $(document).ready(function () {
                $("#btnsubmit").click(function (e) {
                    InsertUpdateArea();
                });   
            });




        </script>
    }

</div>